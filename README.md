# Flowpage (version HTML statique)

Le site a été converti d'une implémentation React vers une page unique **HTML + CSS** (`index.html`).

## Lancer en local

```bash
npm install
npm run dev
```

Puis ouvrir l'URL affichée par Vite.
